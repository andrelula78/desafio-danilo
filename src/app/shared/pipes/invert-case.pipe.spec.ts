import { InvertCasePipe } from './invert-case.pipe';

describe('InvertCasePipe', () => {
  it('create an instance', () => {
    const pipe = new InvertCasePipe();
    expect(pipe).toBeTruthy();
  });
});
